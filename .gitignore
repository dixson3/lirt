# Gas Town catalog items (managed at rig level, not in repo)
roles/ambassador.md
roles/chronicler.md
roles/code-quality-reporter.md
roles/test-case-identifier.md
.claude/agents/chronicler.md
.claude/settings.json

# Catalog scripts (note: bin/ already gitignored below for Go builds)
# bin/lirt-ambassador, bin/lirt-chronicler, etc. covered by bin/

# lirt runtime configuration (town-specific)
.lirt/

# Beads runtime files
.beads/beads.db*
.beads/daemon.*
.beads/sync_base.jsonl
.beads/last-touched
.beads/.sync.lock
.beads/.local_version
.beads/redirect

# Go build artifacts
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/

# Go coverage
*.coverprofile
coverage.out
coverage.html

# Environment files with secrets
.env.test
.env.test.local
*.env.test

# OS and editor files
.DS_Store
*~
*.swp
*.swo
.vscode/
.idea/
